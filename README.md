# SpaceTaxi #

```bash
cp .env.example .env
php artisan key:generate
composer install
npm install
```

* update your .env
* set up your database

```bash
php artisan migrate --seed
php artisan serve
```
